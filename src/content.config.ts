// Content collections config (SCF-01). Real entry schemas — the shared
// `data` + locale-keyed `prose` split, `fitment`, `confidence`, `sources` —
// land in T104. This scaffold only wires the file up so `astro check` and
// `astro build` can resolve `astro:content` from day one.
export const collections = {};
