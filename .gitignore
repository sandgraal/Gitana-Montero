# deps / build
node_modules/
dist/
.astro/
coverage/
playwright-report/
test-results/

# Vercel CLI scratch: `.vercel/project.json` carries the org and project IDs
# this checkout is linked to, plus pulled environment values. Never committed.
.vercel/

# env & secrets
.env
.env.*
!.env.example

# local tooling
.claude/settings.local.json
.claude/worktrees/
*.local

# OS
.DS_Store
